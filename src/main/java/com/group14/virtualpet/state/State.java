package com.group14.virtualpet.state;

// TODO: Add import for Pet class when needed

/**
 * Interface or abstract class representing a pet's state (e.g., Normal, Sleeping).
 * TODO: Define common state behaviors (e.g., handleFeed, handlePlay).
 */
public interface State {
    // TODO: Define interface methods for state-specific actions
    // Example: void feed(Pet pet);
    // Example: void play(Pet pet);
} 